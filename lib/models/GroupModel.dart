class GroupModel {
  String groupName;
  String groupImg;
  String lastUser;
  String lastMessage;
  int unreadCount;
  GroupModel(this.groupName, this.groupImg, this.lastUser, this.lastMessage,
      this.unreadCount);
}
